/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

//CODE HERE
let gitDefinition = "noun DEROGATORY•INFORMAL an unpleasant or contemptible person. ex'that mean old git' JKJK!!! It is a version control system. It allows individuals and teams to track changes to code projects."
//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

//CODE HERE
let gitHubDefinition = "gitHub is a cloud storage site for one to upload and house coding repositories. It allows for teams to easily share and view code."
//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

//CODE HERE
let gitInitDefinition = "git init is a command used in the git terminal that initializes an empty repo for a given folder/directory."
//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

//CODE HERE
let gitCloneDefinition = "git clone is a command used in the git terminal that clones a repo from github onto your personal computer. You would git clone --githubrepourl--"
//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

//CODE HERE
let gitStatusDefinition = "a command used in the git terminal that allows the user to view the current status of the files in the given folder/directory. It shows whether the files are being tracked, ready to be committed, or if the branch is clean."
//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE
let gitAddDefinition = "a command used in the git terminal that allows the user to 'add' a file to the local git repo. The computer will then track the file, and allow the file to be committed."
let gitAddCode = "git add -a or git add ."
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
let gitCommitDefinition = "a command used in the git terminal that will prepare a given code to be pushed to github. It also accepts a message, allowing a user to see what was changed."
let gitCommitCode = "git commit -m 'initial commit'"

//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE
let gitPushDefinition = "a command used in the git terminal that will push given code, folder, or directory up to gitHub. A user would use this after they have linked their local repo to the repo created in gitHub."